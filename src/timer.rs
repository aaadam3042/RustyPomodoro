// Enum to keep timer states
enum TimerStates {
    Idle,
    CountDown,
    Paused,
}

pub struct Timer {}

impl Timer {
    pub fn new() -> Self {
        Self {

        }
    }

    pub fn start(&self) {

    }
}